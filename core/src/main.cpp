/**
 * transit-engine — Multi-modal transit routing engine
 *
 * Entry point for the core routing service.
 */

#include <iostream>

int main(int argc, char* argv[]) {
    std::cout << "transit-engine v0.1.0 — starting routing service…\n";
    return 0;
}
